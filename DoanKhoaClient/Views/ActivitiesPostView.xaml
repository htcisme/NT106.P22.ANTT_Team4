<Window
    x:Class="DoanKhoaClient.Views.ActivitiesPostView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:DoanKhoaClient.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:DoanKhoaClient.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Light_ActivitiesPostView"
    Title="ActivitiesPostView"
    MinWidth="1024"
    MinHeight="768"
    WindowStartupLocation="CenterScreen"
    WindowState="Maximized"
    mc:Ignorable="d">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />




        <!--  Style for comment input  -->
        <Style x:Key="CommentInputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="MaxHeight" Value="100" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer
                            x:Name="PART_ContentHost"
                            Focusable="false"
                            HorizontalScrollBarVisibility="Hidden"
                            VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NavigationButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="MenuItemStyle" TargetType="{x:Type StackPanel}">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TransformGroup>
                        <ScaleTransform ScaleX="1" ScaleY="1" />
                        <TranslateTransform />
                    </TransformGroup>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"
                                To="1.05"
                                Duration="0:0:0.1" />
                            <DoubleAnimation
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"
                                To="1.05"
                                Duration="0:0:0.1" />
                            <DoubleAnimation
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.X)"
                                To="3"
                                Duration="0:0:0.1" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)"
                                To="1"
                                Duration="0:0:0.1" />
                            <DoubleAnimation
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"
                                To="1"
                                Duration="0:0:0.1" />
                            <DoubleAnimation
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(TranslateTransform.X)"
                                To="0"
                                Duration="0:0:0.1" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFE3F6FF" />
            <Setter Property="Foreground" Value="#FF12305E" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="15,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Focusable="false"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True" />
                                    <Condition Property="IsFocused" Value="False" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="#FFE3F6FF" />
                                <Setter TargetName="border" Property="BorderThickness" Value="0" />
                            </MultiTrigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFE3F6FF" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF12305E" />
                                <Setter TargetName="border" Property="BorderThickness" Value="1" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsFocused" Value="False" />
                                    <Condition Property="IsMouseOver" Value="False" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="#FFE3F6FF" />
                                <Setter TargetName="border" Property="BorderThickness" Value="0" />
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SortButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#7FFFFFFF" />
            <Setter Property="Foreground" Value="#FF12305E" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#FF12305E" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                            <ContentPresenter
                                x:Name="contentPresenter"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFE2F5FF" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF12305E" />
                                <Setter Property="Foreground" Value="#FF12305E" />

                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFB3E0F7" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF0A1D4A" />
                                <Setter Property="Foreground" Value="#FF0A1D4A" />
                                <Setter TargetName="border" Property="Effect" Value="{x:Null}" />
                                <Setter TargetName="contentPresenter" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="1" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CommentContainerStyle" TargetType="Border">
            <Setter Property="Margin" Value="0,0,0,15" />
            <Setter Property="Padding" Value="15,10" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Background" Value="#EAF6FF" />
            <Style.Triggers>
                <!--  Different style for replies  -->
                <DataTrigger Binding="{Binding IsReply}" Value="True">
                    <Setter Property="Background" Value="#F0F8FF" />
                    <Setter Property="BorderBrush" Value="#E3F2FD" />
                    <Setter Property="BorderThickness" Value="2,0,0,0" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CommentActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFE3F2FD" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FFBBDEFB" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  Reply indicator style  -->
        <Style x:Key="ReplyIndicatorStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="#FF888888" />
            <Setter Property="FontStyle" Value="Italic" />
            <Setter Property="Margin" Value="0,0,0,5" />
        </Style>

        <!--  Time display style  -->
        <Style x:Key="TimeDisplayStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="#6c757d" />
            <Setter Property="Margin" Value="10,0,0,0" />
        </Style>

        <!--  User name style  -->
        <Style x:Key="UserNameStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#FF042354" />
        </Style>

        <!--  Rest of your existing styles...  -->
        <Style x:Key="CommentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFE3F2FD" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FFBBDEFB" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SearchResultButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="border"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFE2F5FF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFB3E0F7" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray" />
                                <Setter TargetName="border" Property="Background" Value="Transparent" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!--  Style cho sidebar button hover  -->
        <Style x:Key="SidebarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#FFE2F5FF" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#B3E0F7" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Storyboard x:Key="ContentFadeInStoryboard">
            <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                From="0"
                To="1"
                Duration="0:0:0.7">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="ImageEnterStoryboard">
            <DoubleAnimation
                BeginTime="0:0:0.3"
                Storyboard.TargetProperty="Opacity"
                From="0"
                To="1"
                Duration="0:0:0.7" />
            <DoubleAnimation
                BeginTime="0:0:0.3"
                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                From="0.9"
                To="1"
                Duration="0:0:0.7">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation
                BeginTime="0:0:0.3"
                Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                From="0.9"
                To="1"
                Duration="0:0:0.7">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>
    <Grid x:Name="ActivitiesPost_Background">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                <GradientStop Offset="0.596" Color="#FFF6FCFF" />
                <GradientStop Offset="1" Color="#FFDBF3FF" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Width="1440" Height="1024">
            <TextBox
                x:Name="Activities_tbSearch"
                Width="307"
                Height="50"
                Margin="355,41,0,0"
                Padding="50,0,20,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                KeyDown="Activities_tbSearch_KeyDown"
                Style="{StaticResource SearchBoxStyle}"
                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />

            <Popup
                IsOpen="{Binding IsSearchResultOpen, Mode=TwoWay}"
                Placement="Bottom"
                PlacementTarget="{Binding ElementName=Activities_tbSearch}"
                StaysOpen="False">
                <Border
                    x:Name="SearchResultsBorder"
                    Width="{Binding ElementName=Activities_tbSearch, Path=ActualWidth}"
                    Background="#FFEDF9FF"
                    BorderThickness="1"
                    CornerRadius="5">
                    <ListBox
                        MaxHeight="200"
                        Background="White"
                        BorderThickness="0"
                        ItemsSource="{Binding SearchResults, UpdateSourceTrigger=PropertyChanged}"
                        ListBox.SelectionMode="Single"
                        ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Button
                                    Height="30"
                                    MinWidth="220"
                                    Margin="0,4,0,4"
                                    Command="{Binding DataContext.OpenActivityDetailCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Content="{Binding Title}"
                                    Style="{StaticResource SearchResultButtonStyle}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
            </Popup>

            <WrapPanel
                Width="493"
                Height="74"
                Margin="743,46,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Orientation="Horizontal">
                <!--  Tag filter đã chọn
    <ItemsControl Margin="10,0,0,0" ItemsSource="{Binding SelectedFilterTags}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border
                    Margin="0,0,8,8"
                    Padding="6,2"
                    Background="#FF12305E"
                    CornerRadius="5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,4,0"
                            Foreground="White"
                            Text="{Binding}" />
                        <Button
                            Width="16"
                            Height="16"
                            Padding="0"
                            Background="Transparent"
                            BorderThickness="0"
                            Command="{Binding DataContext.RemoveFilterTagCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}"
                            Content="✕"
                            Foreground="White" />
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>-->
                <!--  Nút mở dropdown  -->
                <!--  Dropdown filter  -->
                <Popup
                    IsOpen="{Binding IsFilterDropdownOpen, Mode=TwoWay}"
                    Placement="Bottom"
                    PlacementTarget="{Binding ElementName=FilterDropdownButton}"
                    StaysOpen="False">
                    <Popup.Resources>
                        <Storyboard x:Key="PopupFadeIn">
                            <DoubleAnimation
                                Storyboard.TargetProperty="Opacity"
                                From="0"
                                To="1"
                                Duration="0:0:0.2" />
                        </Storyboard>
                    </Popup.Resources>
                    <Border
                        Padding="16"
                        Background="#E6F0FA"
                        BorderBrush="#CCC"
                        BorderThickness="1"
                        Loaded="FilterPopupBorder_Loaded">
                        <StackPanel>
                            <TextBlock
                                Margin="0,0,0,8"
                                FontSize="16"
                                FontWeight="Bold"
                                Text="Bộ lọc" />
                            <TextBlock
                                Margin="0,8,0,4"
                                FontWeight="Bold"
                                Text="Loại hoạt động" />
                            <ItemsControl ItemsSource="{Binding ActivityTypeOptions}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding Display}" IsChecked="{Binding IsChecked, Mode=TwoWay}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Separator Margin="0,8" />
                            <TextBlock
                                Margin="0,8,0,4"
                                FontWeight="Bold"
                                Text="Trạng thái" />
                            <ItemsControl ItemsSource="{Binding ActivityStatusOptions}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding Display}" IsChecked="{Binding IsChecked, Mode=TwoWay}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Button
                                Margin="0,12,0,0"
                                Command="{Binding ApplyFilterCommand}"
                                Content="Lọc" />
                        </StackPanel>
                    </Border>
                </Popup>

            </WrapPanel>
            <Border
                x:Name="ActivitiesPost_Dashboard"
                Width="288"
                Height="942"
                Margin="43,0,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                BorderBrush="Black"
                CornerRadius="10,10,10,10">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Offset="0.04" Color="#FFF6FCFF" />
                        <GradientStop Offset="0.46" Color="#FFDBF3FF" />
                        <GradientStop Offset="0.937" Color="#FF87ACCD" />
                    </LinearGradientBrush>
                </Border.Background>
            </Border>
            <Image
                x:Name="HomePage_iUsers"
                Grid.Column="1"
                Width="40"
                Height="40"
                Margin="1358,46,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Source="/Views/Images/generic avatar.png" />
            <Image
                x:Name="HomePage_iNotifications"
                Width="40"
                Height="40"
                Margin="1297,46,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Source="/Views/Images/light-notifications.png" />
            <Image
                x:Name="ThemeToggleButton"
                Width="40"
                Height="40"
                Margin="1236,46,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                MouseDown="ThemeToggleButton_MouseDown"
                Source="/Views/Images/dark.png" />
            <Image
                x:Name="LightHomePage_iSearch"
                Width="30"
                Height="30"
                Margin="368,51,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Source="/Views/Images/search.png" />
            <Image
                x:Name="HomePage_iLogo"
                Width="112.38"
                Height="40"
                Margin="131,71,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Source="/Views/Images/header.png" />
            <StackPanel>
                <Button
                    x:Name="SidebarHomeButton"
                    Width="160"
                    Height="30"
                    Margin="102,203,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Background="Transparent"
                    BorderThickness="0"
                    Click="SidebarHomeButton_Click"
                    Cursor="Hand"
                    Style="{StaticResource NavigationButtonStyle}">
                    <StackPanel Orientation="Horizontal" Style="{StaticResource MenuItemStyle}">
                        <Image
                            x:Name="HomePage_iHome"
                            Width="30"
                            Height="30"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Source="/Views/Images/home.png" />
                        <Label
                            x:Name="HomePage_lbHome"
                            Width="108"
                            Height="30"
                            Margin="30,0,0,0"
                            Padding="0,0,0,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Content="Home"
                            FontFamily="Segoe UI"
                            FontSize="20"
                            FontWeight="Bold" />
                    </StackPanel>
                </Button>
            </StackPanel>
            <Button
                x:Name="SidebarChatButton"
                Width="160"
                Height="30"
                Margin="102,279,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="Transparent"
                BorderThickness="0"
                Click="SidebarChatButton_Click"
                Cursor="Hand"
                Style="{StaticResource NavigationButtonStyle}">
                <StackPanel Orientation="Horizontal" Style="{StaticResource MenuItemStyle}">
                    <Image
                        x:Name="HomePage_iChat"
                        Width="30"
                        Height="30"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Source="/Views/Images/chat_bubble.png" />
                    <Label
                        x:Name="HomePage_lbChat"
                        Width="108"
                        Height="30"
                        Margin="30,0,0,0"
                        Padding="0,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Content="Chat"
                        FontFamily="Segoe UI"
                        FontSize="20"
                        FontWeight="Bold" />
                </StackPanel>
            </Button>
            <Button
                x:Name="SidebarActivitiesButton"
                Width="160"
                Height="30"
                Margin="102,354,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="Transparent"
                BorderThickness="0"
                Click="SidebarActivitiesButton_Click"
                Cursor="Hand"
                Style="{StaticResource NavigationButtonStyle}">
                <StackPanel Orientation="Horizontal" Style="{StaticResource MenuItemStyle}">
                    <Image
                        x:Name="HomePage_Activities"
                        Width="30"
                        Height="30"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Source="/Views/Images/active-activities.png" />
                    <Label
                        x:Name="HomePage_lbActivities"
                        Width="108"
                        Height="30"
                        Margin="30,0,0,0"
                        Padding="0,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Content="Activities"
                        FontFamily="Segoe UI"
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="#597CA2" />
                </StackPanel>
            </Button>
            <Button
                x:Name="SidebarMemberButton"
                Width="160"
                Height="30"
                Margin="102,432,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="Transparent"
                BorderThickness="0"
                Click="SidebarMembersButton_Click"
                Cursor="Hand"
                Style="{StaticResource NavigationButtonStyle}">
                <StackPanel Orientation="Horizontal" Style="{StaticResource MenuItemStyle}">
                    <Image
                        x:Name="HomePage_Task_iMembers"
                        Width="30"
                        Height="30"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Source="/Views/Images/members.png" />
                    <Label
                        x:Name="HomePage_lbMembers"
                        Width="108"
                        Height="30"
                        Margin="30,0,0,0"
                        Padding="0,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Content="Members"
                        FontFamily="Segoe UI"
                        FontSize="20"
                        FontWeight="Bold" />
                </StackPanel>
            </Button>
            <Button
                x:Name="SidebarTaskButton"
                Width="160"
                Height="30"
                Margin="102,510,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="Transparent"
                BorderThickness="0"
                Click="SidebarTasksButton_Click"
                Cursor="Hand"
                Style="{StaticResource NavigationButtonStyle}">
                <StackPanel Orientation="Horizontal" Style="{StaticResource MenuItemStyle}">
                    <Image
                        x:Name="HomePage_iTasks"
                        Width="30"
                        Height="30"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Source="/Views/Images/tasks.png" />
                    <Label
                        x:Name="HomePage_lbTasks"
                        Width="108"
                        Height="30"
                        Margin="30,0,0,0"
                        Padding="0,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Content="Tasks"
                        FontFamily="Segoe UI"
                        FontSize="20"
                        FontWeight="Bold" />
                </StackPanel>
            </Button>
            <Button
                x:Name="SidebarAdminButton"
                Width="160"
                Height="30"
                Margin="102,586,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Background="Transparent"
                BorderThickness="0"
                Click="SidebarAdminButton_Click"
                Cursor="Hand"
                Style="{StaticResource NavigationButtonStyle}"
                Visibility="Collapsed">
                <StackPanel Orientation="Horizontal" Style="{StaticResource MenuItemStyle}">
                    <Image
                        x:Name="HomePage_iAdmin"
                        Width="30"
                        Height="30"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Source="/Views/Images/admin_panel.png" />
                    <Label
                        x:Name="HomePage_lbAdmin"
                        Width="108"
                        Height="30"
                        Margin="30,0,0,0"
                        Padding="0,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Content="Admin"
                        FontFamily="Segoe UI"
                        FontSize="20"
                        FontWeight="Bold" />
                </StackPanel>
            </Button>
            <!--  Admin submenu  -->
            <StackPanel
                x:Name="AdminSubmenu"
                Margin="132,621,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Visibility="Collapsed">
                <Button
                    x:Name="AdminTaskButton"
                    Margin="0,5,0,5"
                    Background="Transparent"
                    BorderThickness="0"
                    Click="AdminTaskButton_Click"
                    Cursor="Hand">
                    <TextBlock
                        FontSize="15"
                        Foreground="#042354"
                        Text="Quản lý công việc" />
                </Button>
                <Button
                    x:Name="AdminMembersButton"
                    Margin="0,5,0,5"
                    Background="Transparent"
                    BorderThickness="0"
                    Click="AdminMembersButton_Click"
                    Cursor="Hand">
                    <TextBlock
                        FontSize="15"
                        Foreground="#042354"
                        Text="Quản lý thành viên" />
                </Button>
                <Button
                    x:Name="AdminChatButton"
                    Margin="0,5,0,5"
                    Background="Transparent"
                    BorderThickness="0"
                    Click="AdminChatButton_Click"
                    Cursor="Hand">
                    <TextBlock
                        FontSize="15"
                        Foreground="#042354"
                        Text="Quản lý chat" />
                </Button>
                <Button
                    x:Name="AdminActivitiesButton"
                    Margin="0,5,0,5"
                    Background="Transparent"
                    BorderThickness="0"
                    Click="AdminActivitiesButton_Click"
                    Cursor="Hand">
                    <TextBlock
                        FontSize="15"
                        Foreground="#042354"
                        Text="Quản lý hoạt động" />
                </Button>
            </StackPanel>
            <Image
                x:Name="HomePage_iFacebook"
                Width="24"
                Height="24"
                Margin="175,926,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Source="/Views/Images/facebook.png" />
            <Image
                x:Name="HomePage_iInstagram"
                Width="24"
                Height="24"
                Margin="135,926,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Source="/Views/Images/instagram.png" />
            <Image
                x:Name="HomePage_iYoutube"
                Width="24"
                Height="24"
                Margin="215,926,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Source="/Views/Images/youtube.png" />

            <!--  Nội dung bài đăng (Phần mới thêm)  -->
            <Border
                Width="1042"
                Height="864"
                Margin="356,0,0,40"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Panel.ZIndex="-1"
                CornerRadius="10,10,10,10">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Offset="0.04" Color="#FFF6FCFF" />
                        <GradientStop Offset="0.46" Color="#FFDBF3FF" />
                        <GradientStop Offset="0.937" Color="#FF87ACCD" />
                    </LinearGradientBrush>
                </Border.Background>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!--  Nút quay lại  -->
                        <Button
                            Width="120"
                            Height="36"
                            Margin="20,20,0,0"
                            HorizontalAlignment="Left"
                            Click="BackButton_Click"
                            Content="Quay lại"
                            Style="{StaticResource SortButtonStyle}">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5" />
                                </Style>
                            </Button.Resources>
                        </Button>

                        <!--  Tiêu đề bài đăng  -->
                        <TextBlock
                            Width="644"
                            Margin="0,30,0,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Top"
                            FontSize="24"
                            FontWeight="Bold"
                            Foreground="#12305E"
                            Text="{Binding Activity.Title}"
                            TextAlignment="Center"
                            TextWrapping="Wrap" />

                        <!--  Ngày đăng  -->
                        <TextBlock
                            Margin="20,10,20,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Top"
                            FontSize="14"
                            Foreground="#6c757d"
                            Text="{Binding Activity.Date, StringFormat='{}{0:dddd - dd/MM/yyyy}'}" />

                        <!--  Ảnh minh họa  -->
                        <Image
                            MaxWidth="500"
                            Margin="0,20,0,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Top"
                            Source="{Binding Activity.ImgUrl}"
                            Stretch="Uniform" />

                        <!--  Nội dung bài viết  -->
                        <TextBlock
                            Margin="50,30,50,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            FontSize="15"
                            Foreground="#12305E"
                            Text="{Binding Activity.Description}"
                            TextWrapping="Wrap" />

                        <!--  Nút tương tác  -->
                        <StackPanel Margin="50,30,50,0" Orientation="Horizontal">
                            <Button
                                Margin="0,0,20,0"
                                Padding="15,8"
                                Background="Transparent"
                                BorderThickness="0"
                                Command="{Binding DataContext.ParticipateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding Activity.Id}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontSize="20"
                                        Foreground="#597CA2">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="👤" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Activity.IsParticipated}" Value="True">
                                                        <Setter Property="Text" Value="✔" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock
                                        Margin="8,0,0,0"
                                        VerticalAlignment="Center"
                                        Text="{Binding Activity.ParticipantCount}" />
                                </StackPanel>
                            </Button>

                            <Button
                                Padding="15,8"
                                Background="Transparent"
                                BorderThickness="0"
                                Command="{Binding DataContext.LikeCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding Activity.Id}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock VerticalAlignment="Center" FontSize="20">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="♡" />
                                                <Setter Property="Foreground" Value="#597CA2" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Activity.IsLiked}" Value="True">
                                                        <Setter Property="Text" Value="♥" />
                                                        <Setter Property="Foreground" Value="#E53935" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock
                                        Margin="8,0,0,0"
                                        VerticalAlignment="Center"
                                        Text="{Binding Activity.LikeCount}" />
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <!--  Separator  -->
                        <Rectangle
                            Height="1"
                            Margin="20,30,20,0"
                            VerticalAlignment="Top"
                            Fill="#FF042354" />

                        <!--  Thay thế phần Comment section trong ActivitiesPostView.xaml  -->

                        <!--  Comment section  -->
                        <StackPanel Margin="50,30,50,30" VerticalAlignment="Bottom">
                            <TextBlock
                                Margin="0,0,0,15"
                                FontSize="18"
                                FontWeight="Bold"
                                Foreground="#042354"
                                Text="Bình luận" />

                            <!--  Loading indicator for comments  -->
                            <TextBlock
                                Margin="0,0,0,10"
                                FontSize="14"
                                Foreground="#6c757d"
                                Text="Đang tải bình luận..."
                                Visibility="{Binding IsLoadingComments, Converter={StaticResource BooleanToVisibilityConverter}}" />

                            <!--  Comments list  -->
                            <!--  Replace the Comments ItemsControl in ActivitiesPostView.xaml  -->

                            <!--  Comments list with improved reply visualization  -->
                            <ItemsControl ItemsSource="{Binding Comments}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,15">
                                            <Grid.ColumnDefinitions>
                                                <!--  Indentation column for replies  -->
                                                <ColumnDefinition Width="{Binding IndentLevel, Converter={StaticResource IndentToWidthConverter}}" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <!--  Reply indicator line  -->
                                            <Border
                                                Grid.Column="0"
                                                Width="2"
                                                Margin="0,0,10,0"
                                                HorizontalAlignment="Right"
                                                Background="#FFE0E0E0"
                                                Visibility="{Binding IsReply, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                            <!--  User avatar  -->
                                            <Border
                                                Grid.Column="1"
                                                Width="40"
                                                Height="40"
                                                Background="#E6E6E6"
                                                CornerRadius="20">
                                                <Image
                                                    Source="{Binding UserAvatar}"
                                                    Stretch="UniformToFill"
                                                    Visibility="{Binding UserAvatar, Converter={StaticResource StringToVisibilityConverter}}" />
                                            </Border>

                                            <!--  Comment content  -->
                                            <Border
                                                Grid.Column="2"
                                                Margin="10,0,10,0"
                                                Padding="15,10"
                                                CornerRadius="8">
                                                <!--  Different background for replies  -->
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#EAF6FF" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsReply}" Value="True">
                                                                <Setter Property="Background" Value="#F0F8FF" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>

                                                <StackPanel>
                                                    <!--  Reply indicator header  -->
                                                    <StackPanel
                                                        Margin="0,0,0,5"
                                                        Orientation="Horizontal"
                                                        Visibility="{Binding IsReply, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <TextBlock
                                                            FontSize="12"
                                                            FontStyle="Italic"
                                                            Foreground="#FF888888"
                                                            Text="↳ Phản hồi" />
                                                    </StackPanel>

                                                    <!--  User name and time  -->
                                                    <StackPanel Margin="0,0,0,5" Orientation="Horizontal">
                                                        <TextBlock
                                                            FontWeight="SemiBold"
                                                            Foreground="#FF042354"
                                                            Text="{Binding UserDisplayName}" />

                                                        <!--  Comment type indicator  -->
                                                        <Border
                                                            Margin="8,0,8,0"
                                                            Padding="4,1"
                                                            CornerRadius="3"
                                                            Visibility="{Binding IsReply, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Background" Value="#FFE3F2FD" />
                                                                </Style>
                                                            </Border.Style>
                                                            <TextBlock
                                                                FontSize="10"
                                                                FontWeight="Medium"
                                                                Foreground="#FF1976D2"
                                                                Text="Phản hồi" />
                                                        </Border>

                                                        <!--  Time with tooltip showing detailed time  -->
                                                        <TextBlock
                                                            Margin="10,0,0,0"
                                                            FontSize="12"
                                                            Foreground="#6c757d"
                                                            Text="{Binding TimeAgo}"
                                                            ToolTip="{Binding DetailedTime}" />
                                                    </StackPanel>

                                                    <!--  Comment text  -->
                                                    <TextBlock
                                                        Margin="0,0,0,8"
                                                        Text="{Binding Content}"
                                                        TextWrapping="Wrap" />

                                                    <!--  Action buttons  -->
                                                    <StackPanel Orientation="Horizontal">
                                                        <!--  Like button  -->
                                                        <Button
                                                            Margin="0,0,15,0"
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            Command="{Binding DataContext.LikeCommentCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Cursor="Hand">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock FontSize="12">
                                                                    <TextBlock.Style>
                                                                        <Style TargetType="TextBlock">
                                                                            <Setter Property="Text" Value="♡" />
                                                                            <Setter Property="Foreground" Value="#0E4C92" />
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding IsLiked}" Value="True">
                                                                                    <Setter Property="Text" Value="♥" />
                                                                                    <Setter Property="Foreground" Value="#E53935" />
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </TextBlock.Style>
                                                                </TextBlock>
                                                                <TextBlock
                                                                    Margin="4,0,0,0"
                                                                    FontSize="12"
                                                                    Foreground="#0E4C92"
                                                                    Text="Thích" />
                                                                <TextBlock
                                                                    Margin="4,0,0,0"
                                                                    FontSize="12"
                                                                    Foreground="#0E4C92"
                                                                    Text="{Binding LikeCount}"
                                                                    Visibility="{Binding LikeCount, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}" />
                                                            </StackPanel>
                                                        </Button>

                                                        <!--  Reply button - Hide for replies to prevent nested replies  -->
                                                        <Button
                                                            Margin="0,0,15,0"
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            Command="{Binding DataContext.ReplyToCommentCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Cursor="Hand"
                                                            Visibility="{Binding IsReply, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                                            <TextBlock
                                                                FontSize="12"
                                                                Foreground="#0E4C92"
                                                                Text="Phản hồi" />
                                                        </Button>

                                                        <!--  Delete button (only for comment owner)  -->
                                                        <Button
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            Command="{Binding DataContext.DeleteCommentCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Cursor="Hand"
                                                            Visibility="{Binding IsOwner, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <TextBlock
                                                                FontSize="12"
                                                                Foreground="#E53935"
                                                                Text="Xóa" />
                                                        </Button>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>

                                            <!--  Options menu  -->
                                            <StackPanel
                                                Grid.Column="3"
                                                Orientation="Horizontal"
                                                Visibility="{Binding IsOwner, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock
                                                    VerticalAlignment="Top"
                                                    Cursor="Hand"
                                                    FontSize="16"
                                                    FontWeight="Bold"
                                                    Foreground="#6c757d"
                                                    Text="⋯" />
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!--  Reply indicator  -->
                            <Border
                                Margin="0,10,0,10"
                                Padding="10,5"
                                Background="#FFE3F2FD"
                                CornerRadius="5"
                                Visibility="{Binding ReplyingToComment, Converter={StaticResource NullToVisibilityConverter}}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock
                                        FontSize="12"
                                        Foreground="#FF12305E"
                                        Text="Đang phản hồi: " />
                                    <TextBlock
                                        FontSize="12"
                                        FontWeight="Bold"
                                        Foreground="#FF12305E"
                                        Text="{Binding ReplyingToComment.UserDisplayName}" />
                                    <Button
                                        Margin="10,0,0,0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Command="{Binding CancelReplyCommand}"
                                        Content="Hủy"
                                        FontSize="12"
                                        Foreground="#E53935" />
                                </StackPanel>
                            </Border>

                            <!--  Comment input  -->
                            <Grid Margin="0,20,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!--  Current user avatar  -->
                                <Border
                                    Grid.Column="0"
                                    Width="40"
                                    Height="40"
                                    Background="#E6E6E6"
                                    CornerRadius="20">
                                    <!--  You can bind this to current user's avatar  -->
                                </Border>

                                <!--  Comment input box  -->
                                <Border
                                    Grid.Column="1"
                                    Margin="10,0,10,0"
                                    Padding="15,10"
                                    Background="#EAF6FF"
                                    CornerRadius="8">
                                    <TextBox
                                        x:Name="CommentTextBox"
                                        VerticalAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontSize="14"
                                        Text="{Binding NewCommentText, UpdateSourceTrigger=PropertyChanged}"
                                        TextWrapping="Wrap">
                                        <TextBox.Style>
                                            <Style TargetType="TextBox">
                                                <Style.Triggers>
                                                    <Trigger Property="Text" Value="">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <VisualBrush
                                                                    AlignmentX="Left"
                                                                    AlignmentY="Center"
                                                                    Stretch="None">
                                                                    <VisualBrush.Visual>
                                                                        <TextBlock
                                                                            FontSize="14"
                                                                            Foreground="#6c757d"
                                                                            Text="Viết bình luận..." />
                                                                    </VisualBrush.Visual>
                                                                </VisualBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBox.Style>
                                    </TextBox>
                                </Border>

                                <!--  Post button  -->
                                <Button
                                    Grid.Column="2"
                                    Padding="12,8"
                                    Background="#FF12305E"
                                    BorderThickness="0"
                                    Command="{Binding PostCommentCommand}"
                                    Foreground="White"
                                    IsEnabled="{Binding NewCommentText, Converter={StaticResource StringNotEmptyToBoolConverter}}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border
                                                            Padding="{TemplateBinding Padding}"
                                                            Background="{TemplateBinding Background}"
                                                            CornerRadius="5">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#FF4A6483" />
                                                            </Trigger>
                                                            <Trigger Property="IsPressed" Value="True">
                                                                <Setter Property="Background" Value="#FF3C536C" />
                                                            </Trigger>
                                                            <Trigger Property="IsEnabled" Value="False">
                                                                <Setter Property="Background" Value="#FFCCCCCC" />
                                                                <Setter Property="Foreground" Value="#FF888888" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                    <TextBlock Text="Đăng" />
                                </Button>
                            </Grid>
                        </StackPanel>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>